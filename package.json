{
  "name": "snip-notes",
  "displayName": "Snip Notes",
  "description": "Note taking for vs-code",
  "publisher": "gziz",
  "repository": "https://github.com/gziz/snip-notes.git",
  "version": "0.0.4",
  "engines": {
    "vscode": "^1.81.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./src/extension.js",
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "snipNotes",
          "title": "Snip Notes",
          "icon": "resources/snip-notes.svg"
        }
      ]
    },
    "views": {
      "snipNotes": [
        {
          "type": "webview",
          "id": "snipNotes.webView",
          "name": "Block Notes"
        },
        {
          "id": "snipNotes.treeView",
          "name": "Tree Notes"
        }
      ]
    },
    "menus": {
      "webview/context": [
        {
          "command": "snip-notes.deleteNote",
          "when": "webviewId == 'snipNotes.webView' && webviewSection == 'noteBlock'"
        }
      ]
    },
    "commands": [
      {
        "command": "snip-notes.createNote",
        "title": "Create Note"
      },
      {
        "command": "snip-notes.deleteNote",
        "title": "Delete Note"
      },
      {
        "command": "snip-notes.refreshNotes",
        "title": "Refresh Notes"
      }
    ]
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "node ./test/runTest.js"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.1",
    "@types/node": "16.x",
    "@types/vscode": "^1.81.0",
    "@vscode/test-electron": "^2.3.4",
    "eslint": "^8.47.0",
    "glob": "^10.3.3",
    "mocha": "^10.2.0",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "sql.js": "^1.8.0"
  }
}
